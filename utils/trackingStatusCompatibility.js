const LEGACY_TERMINAL_LAST_LOCATION_STATUS = "offline";

function getLegacyTerminalLastLocationStatus(_semanticStatusKey) {
    return LEGACY_TERMINAL_LAST_LOCATION_STATUS;
}

module.exports = {
    LEGACY_TERMINAL_LAST_LOCATION_STATUS,
    getLegacyTerminalLastLocationStatus
};
